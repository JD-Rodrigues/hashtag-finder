const selectTweetCardInfo = async (hashtag) => {
  
//   const lastTweetsByHashtag = await fetchLastTweets(hashtag)
//   let tweetCardsInfo = []
  

//   const cards = await lastTweetsByHashtag.data.map(async(post)=>{
//     const tweet = await getTweetInfo(post.id)
//     const user = await getUserInfo(post.author_id)
//     // return {
//     //   username: user.data[0].username,
//     //   userPic: user.data[0].profile_image_url,
//     //   text: tweet.data[0].text,
//     //   tweetId: tweet.data[0].id,
//     // }
    
    
//   })
  
  
//   return await cards
  
// }